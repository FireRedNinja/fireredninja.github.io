module.exports = {
  // Extend Gatsby's default config but disable flowtype
  extends: [],
  plugins: [],
  rules: {},
  // Ignore everything - we'll run ESLint manually if needed
  ignorePatterns: ["**/*"],
};
